import styled from "@emotion/styled"

export const ImageBox = styled.img`
    height: 4rem;    
`

export const ImageLink = styled.div`
    padding: 1rem;
    margin: 0 0.5rem 0;
    cursor: pointer;
`
